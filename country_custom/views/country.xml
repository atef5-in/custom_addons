<odoo>

    <record id="view_country_tree" model="ir.ui.view">
        <field name="name">res.country.tree</field>
        <field name="model">res.country</field>
        <field name="arch" type="xml">
            <tree string="Country">
                <field name="name"/>
                <field name="code"/>
            </tree>
        </field>
    </record>

    <record id="view_country_form" model="ir.ui.view">
        <field name="name">res.country.form</field>
        <field name="model">res.country</field>
        <field name="arch" type="xml">
            <form string="Country">
                <field name="image"/>
                <group>
                    <group>
                        <field name="name"/>
                        <field name="currency_id"/>
                    </group>
                    <group>
                        <field name="code"/>
                    </group>
                </group>
                <label for="address_format"/>
                <field name="address_format" groups="base.group_no_one" placeholder="Address format..."/>
            </form>
        </field>
    </record>

    <record id="action_country" model="ir.actions.act_window">
        <field name="name">Pays</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.country</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
    </record>

    <menuitem id="menu_country" name="Pays" parent="menu_country_root" sequence="0" action="action_country"/>

</odoo>