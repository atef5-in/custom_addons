<odoo>

    <record id="view_country_group_tree" model="ir.ui.view">
        <field name="name">res.country.group.tree</field>
        <field name="model">res.country.group</field>
        <field name="arch" type="xml">
            <tree string="Country">
            </tree>
        </field>
    </record>

    <record id="view_country_group_form" model="ir.ui.view">
        <field name="name">res.country.group.form</field>
        <field name="model">res.country.group</field>
        <field name="arch" type="xml">
            <form string="Country">
            </form>
        </field>
    </record>

    <record id="action_group" model="ir.actions.act_window">
        <field name="name">Parcs et Municipalités</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">res.country.group</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
    </record>

    <menuitem id="menu_group" name="Parcs et Municipalités" parent="menu_country_root" sequence="30" action="action_group"/>

</odoo>